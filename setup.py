from setuptools import setup

setup(
    name='flux_fsm',
    version='0.1',
    packages=['flux_fsm'],
    package_dir={'flux_fsm': 'bindings/python'},
    include_package_data=False,
    install_requires=[],
)